# dependencies
/node_modules

# next.js
/.next
/out
next-env.d.ts

# production
/build

# environment — NEVER commit secrets
.env
.env*.local
.env.production

# local sqlite leftovers (dev only — production is PostgreSQL/Neon)
/db/*.db
/db/*.db-journal

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
